# Build resources

Put release icons and installer assets here before publishing:

- `icon.ico` for Windows
- `icon.icns` for macOS
- `icon.png` for Linux or generic metadata

The app can package without these placeholders during prototyping, but signed production builds should include platform-specific icons.
